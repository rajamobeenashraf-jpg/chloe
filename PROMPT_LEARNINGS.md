# PROMPT LEARNINGS — per-model ledger of verified obey/ignore evidence

**Purpose (owner-approved 2026-08-29):** every generation round teaches us what a model
actually obeys, ignores, or mangles. That knowledge prevents regenerations — but only if
it survives the session. This file is the ledger. **Update it after every generation round:**
one line per finding, with the job ID(s) as proof. Findings here are FACTS with evidence;
the EXTERNAL GUIDANCE section is documentation-derived and marked unverified until a
project generation confirms it.

Rule of use: consult this file at §9 stage 8 (prompt writing), before every prompt.

---

## VERIFIED IN THIS PROJECT (evidence = job IDs in the episode production logs)

### Seedance 2.5 (`seedance_2_5`)
| # | Finding | Evidence | Date |
|---|---|---|---|
| S1 | `t2v` mode IGNORES placement geometry ("wedge from frame-left striking a line right-of-center" rendered as two head-on columns). Text alone cannot place masses. | 6a v1 `a7ba2c6b` | 2026-08-29 |
| S2 | **Start-frame method WORKS:** lock composition in a still, generate via `omni_reference` — geometry, gear separation, and dust state all carried into motion. THE standard for hard-geometry shots. | still `9dc96a94` → 6a v2 `63fa03dd` | 2026-08-29 |
| S3 | Bare negation of fine costume detail is unreliable: "helmets WITHOUT crests / no crests" ignored. Describe the POSITIVE alternative instead (name the exact helmet type and shape); or fix it in the reference still — note the still generator ignored it too (see N1). | `9dc96a94`, `63fa03dd` | 2026-08-29 |
| S4 | Frozen ENVIRONMENT_BLOCK pasted verbatim → scale executed superbly (lines past frame edges, thousands, layered haze). Block-injection works; keep it mandatory. | `a7ba2c6b`, `63fa03dd` | 2026-08-29 |
| S5 | Clip-opening state is under-obeyed in t2v ("air ALREADY heavy with dust" → clean first second). Mitigation: bake the required state into the start still (v2 confirmed much better). | v1 vs v2 | 2026-08-29 |
| S6 | `omni_reference` coerces `start_image` role → `reference_images` (seen in params echo): the still acts as a strong composition anchor, not a guaranteed literal first frame. Expect adherence, not pixel-identity. | `63fa03dd` submit echo | 2026-08-29 |
| S7 | Preset intercept (`preset_recommendation`) on cinematic prompts → resubmit identically with `declined_preset_id`. | `a7ba2c6b` round | 2026-08-29 |
| S8 | `t2v` rejects reference media outright (422) — any refs require `mode:"omni_reference"`. | Constantinople §31 | 2026-08 |
| S9 | Multi-panel character-sheet GRIDS are rejected/harmful as references — single-view images only (2 full-body + 2 face crops per character). | §31 recipe | 2026-08 |
| S10 | **Paraphrasing the frozen ENVIRONMENT_BLOCK instead of pasting it verbatim silently collapses scale — this is a Claude prompting error, confirmed, not a model limitation.** On the multi-angle validation test, a condensed environment paragraph ("thousands in haze at the horizon") replaced the block's mandatory 3-tier layering line (10-15 foreground / dozens-hundreds midground / thousands background) — the result showed only ~6-7 riders at the point of contact, everyone else at a visible remove, not a full-army wave. 6a v2, which pasted the block verbatim, had no such problem. §7b.3 exists specifically to prevent this; it was violated by hand-writing a substitute instead of pasting. | multi-angle test `c0e238ea`, owner-flagged 2026-08-29 | 2026-08-29 |
| S11 | **Close/low-angle shots are the highest scale-risk framing and need an explicit depth-continuation line** ("the charge continues in depth behind these riders, rank after rank, receding into dust") — a close camera naturally reads as "the nearest few" unless told the mass continues beyond what's sharp. Compounds S10 when both are missing at once. | same as S10 | 2026-08-29 |
| S12 | **A true HEAD-ON reverse angle (camera facing directly down the line of an oncoming charge) cannot show formation DEPTH, no matter how the prompt is worded — this is camera geometry, not a prompting gap.** Riders directly behind other riders are occluded along the sightline; depth-continuation language (S11) only produces a WIDER single rank, not stacked ranks, because stacked riders would be invisible from that angle anyway. Confirmed by direct frame comparison: 6a's SIDE-ON angle (roughly perpendicular to the charge) shows multiple offset, visibly stacked ranks receding diagonally — genuine depth; the head-on reverse test showed one wide rank + a small impact cluster, no second rank. **Rule: any reverse/new-angle shot that must ALSO read as a deep formation needs an OBLIQUE offset (angled diagonally, never dead-center facing the action) — never a pure head-on camera.** | test v1 `c0e238ea` vs test v2 `c5fdb474` vs 6a `63fa03dd`, owner-flagged and frame-verified 2026-08-29 | 2026-08-29 |
| S13 | **Telephoto-compressed locked-camera shots concentrate the model's motion budget on the single sharpest foreground layer — ranks/subjects one layer back render as an inert, frozen backdrop unless EACH depth layer is individually scripted as its own moving subject.** Found on 5c: the elevated/oblique/telephoto angle adopted to fix a density/gap problem (see S-series gap findings) had a side effect nobody predicted — five video attempts needed to root-cause it. v1 (thin scene, only 2 subtle motions for 4s) froze almost entirely (freezedetect: frozen 0.5-3.75s of 4.06s). v2 (explicit "camera locked ≠ scene static" clarification) got partial motion (0.8-1.8s) then froze again. v3 (MORE emphatic "don't freeze" prohibition text, no new content) regressed to near-total freeze again — proving that piling on negative/prohibition instructions doesn't help once the actual problem is elsewhere. v4 (root cause #1: not enough EVENT CONTENT for the duration — rewrote as one large collective front-rank wave motion, matching 6a's event density) got the best result: real motion 0-1.3s. Direct frame inspection of v4's mid-motion frame (not just freezedetect) found root cause #2: motion blur was visible ONLY on the front 1-2 men; every rank behind them, even though scripted as "readable formation" not indistinct haze, was pixel-static with zero blur the entire clip — the prompt only ever gave "the front rank" as a named moving subject, so the model had nothing to animate at any other depth. v5 (fix attempt: explicitly scripted 3 named ranks in a staggered backward-rippling wave) REGRESSED to near-total freeze — the added multi-part choreography overloaded the prompt past a complexity threshold, same failure mode as v3. **Net finding: (1) freezedetect + direct frame inspection at multiple timestamps is the only reliable way to catch this — a single first/last-frame comparison alone can miss a "motion then freeze" clip; (2) fixing it needs MORE positive event content, not more negative prohibition text (mirrors S3/N1's positive-vs-negative-instruction finding, now confirmed at the motion/timing level too); (3) content added to fix this has a complexity ceiling — one clearly-scripted collective action for the sharp foreground layer outperformed a more anatomically-correct multi-rank staggered version. Accepted outcome on 5c: v4, ~1.3s of real motion out of 4.06s, owner-approved as the ceiling for this shot rather than pursuing further rounds.** | v1 `60cf92c9` → v2 `9a31f1ab` → v3 `8b1f34b8` → v4 `ef3b5276` (ACCEPTED) → v5 `0e659778` | 2026-08-30 |
| S14 | **When a start-frame composition requires two subjects to visually share ONE ground plane, arrange them HORIZONTALLY (same camera depth, different x-position) rather than VERTICALLY (one stacked above the other needing a bridge/gap-fill).** Found on 6b: the still's "two floors" defect (horse read as elevated above the soldiers) was first patched by vertically stretching the horse's leg down to overlap the soldiers' zone — this fixed the STILL, but once Seedance animated motion from a start frame with real horse/human overlap, it fused the horse's leg and a soldier's leg into one physically impossible limb (owner: "horror movie"). Switching to a horizontal split (horse confined to the left third, soldiers to the center-right two-thirds, generated as one native composition, never overlapping) fixed the ground-plane read AND removed the fusion risk entirely, because ordinary perspective — not a forced bridge — puts both subjects' ground-contact points at the same frame-height when they share a depth. Zero compositing needed at the still stage; zero overlap risk at the video stage. **Rule: never force two separate subjects into the same x,y screen space in a start frame to solve a vertical continuity problem — resolve it by repositioning them side by side instead.** | v6 stretch-bridge still → video job `aa3cf0d1` (leg fusion, REJECTED) → v8 horizontal-split still (owner-approved) → video job `e27fbb93`/`ef95508f`/`444a8fef` (no fusion in any of the 3 video attempts on this composition) | 2026-08-31 |
| S15 | **A "don't add a rider" instruction — even repeated verbatim in REFERENCE ROLES, CONTINUITY, and CONSTRAINTS simultaneously — can still be violated TRANSIENTLY mid-clip (a rider's leg/greave briefly visible on the horse's back around 1.5-2s, gone again by the final frame) despite a start frame that has no rider at all.** This reads as a persistent `seedance_2_5` prior that a galloping cavalry horse "should" be mounted, strong enough to occasionally override an explicit, thrice-repeated negative instruction for part of a clip's duration. Unlike S3/N1 (negation failing on costume/appearance DETAILS, fixed by positive description), this is a negation failing on a whole ELEMENT's existence mid-clip on a subject the model has a strong prior about — positive rephrasing ("hooves and legs ONLY") didn't fully suppress it either. Not yet resolved with a reliable fix; logged as a known residual risk for any mounted-horse ground shot. Owner accepted this specific instance (6b v10) despite the flicker rather than pursue further rounds. | 6b v10, job `444a8fef-2e79-4fec-bf35-80f0e4992a51` (owner-approved with this known flaw) | 2026-08-31 |
| S16 | **`freezedetect -30dB` produces large, often near-total-duration false positives on steady single-shot talk-to-lens clips with minimal camera movement — the lower the overall frame-to-frame pixel variance (a locked/near-locked selfie shot showing mostly just a face), the more of the clip gets flagged, even though real speech motion (mouth, eyes, blinks) is continuous throughout.** Confirmed on two clips now: Clip 8 (two-person scene, ~1.75s flagged, later confirmed real via pixel-diff) and Clip 9a (single-character static-camera talk-to-lens, ~12s of a 15s clip flagged across 7 separate freeze segments, essentially the whole clip) — quarter-second frame pixel-diffing across the full 9a clip showed continuous mean differences of ~3-16 throughout (never near-zero) and visibly different mouth/eyebrow positions at every sampled second, so no genuine stuck frame existed despite the alarming freezedetect output. **Standing QC method: whenever freezedetect flags a clip, before treating it as a defect, pixel-diff sampled frames (2-4fps) across the flagged window(s) with PIL/numpy and check the mean difference stays non-trivial (not near-zero) throughout — freezedetect's threshold is tuned for hard cuts/stalls, not low-but-real motion, and is the wrong tool to trust standalone on any single-camera, low-movement composition (steady talk-to-lens is the most common case in this project).** This does not relax the underlying freeze rule — a genuine stuck frame (near-zero diff sustained across the flagged window) is still a real defect requiring the standard fix; this only changes how a freezedetect flag gets verified before acting on it. | Clip 8 job `c0637e0e` (4.375-6.125s flagged, confirmed real motion) → Clip 9a job `4d31ea3b` (0-14.7s flagged across 7 segments, confirmed real motion throughout via 4fps pixel-diff, mean 2.7-16 at every sample) | 2026-08-31 |
| S17 | **Regenerating a clip mid-edit (same dialogue text, new take) invalidates its word-level caption timing even though the script is unchanged — a fresh generation has its own pacing/pause placement, so the old `word_chunks.json`/`captions_data.mjs` timestamps drift out of sync with the new audio.** Found on Clip 4's v2 regen (fixed a Bucephalus scale-jump + a coverage gap, dialogue text untouched): re-running `make_word_chunks.py` against the new audio shifted nearly every word's timing by 0.05-0.8s versus v1's measured chunks (e.g. "THAT ONE —" moved from 8.12-8.50s to 8.30-8.70s) despite 100% script-match on both takes. **Method for a single-clip mid-edit regen, without re-touching already-approved clips' timing:** build a one-entry `lines.json` containing only the changed clip's line-level windows/text, run `make_word_chunks.py` against it alone (writes a standalone chunks file), then merge just that clip's key into the full `word_chunks.json` and hand-edit its block in `captions_data.mjs` — never re-run the batch script across all clips for a single-clip fix, since it would re-transcribe (and risk perturbing) every other clip's audio for no reason. Also re-run `qc_pass.mjs`'s per-clip logic (captions burn + loudnorm + fade + codec/resolution normalize) for the one changed clip only, then rebuild the full cut. | Clip 4 v1 chunks (`word_chunks.json`, measured pre-regen) → v2 chunks re-measured against job `7f770fef`, 100% match both takes, full-clip timing shift | 2026-08-31 |

### nano_banana_pro (routes to nano_banana_2 — stills)
| # | Finding | Evidence | Date |
|---|---|---|---|
| N1 | Also ignores "no crests" negation on helmets (same failure as S3 — cross-model). Positive description required. | `9dc96a94` | 2026-08-29 |
| N4 | **PHOTO-EDIT beats fresh-render for angle coverage:** two fresh-rendered face-3/4s drifted to a different woman (`d6f10ddd`, `14acb9d5`, both owner-rejected) even with doubled refs; framing it as "PHOTO EDIT of the first reference — same photograph, head turned ~25°" held identity on the first try (`b70ab775`, owner-approved). Standard method for multi-view sets and any same-subject variation. | v5 canon build | 2026-08-29 |
| N5 | Multi-view identity drift is the norm, not the exception, when rendering fresh angles from refs: 4 of the first 6 v5 view renders drifted (full-bodies v1+v2, face-3/4 v1+v2). Budget for rejection rounds; owner's eye is the gate. | v5 canon build | 2026-08-29 |
| N6 | Incremental single-change iteration works cleanly on a same-face chain (glow → fair → pink → lips → freckles, 9 steps, identity held throughout when each step named "the ONLY change") — but big anatomical asks (eyes +20%) render subtler than requested; the model resists large feature shifts from a same-face ref. | Rounds 7–7i | 2026-08-29 |
| N2 | Executes complex two-army compositions with placement language well in a STILL (where Seedance t2v fails in motion) — which is why the start-frame method works. | `9dc96a94` | 2026-08-29 |
| N3 | Look/wardrobe drift risk in video is fixed by passing an approved costume STILL as a reference, not by longer text (text-only wardrobe drifted twice on Troy). | Troy log, clip-1 attempts | 2026-08 (Troy) |
| N7 | **Reference-image COUNT is itself a failure risk for stills, independent of language.** Clip 3's start-frame failed twice with no error detail at 14 refs (the `--still` mode's old default: 5 episode-costume + 5 canon-4K Hazel refs, stacked, plus 4 Alexander refs) and again at 4 refs (2 Hazel + 2 Alexander); it succeeded immediately at 2 refs (1 Hazel + 1 Alexander), no other prompt change made between the last two attempts. Consistent with N5's existing multi-view-drift caution and the Round 11 Clip 1 finding (2 refs succeeded where 3 failed) — this project's stills are more reliable with lean reference sets (~2 total) than with maximal identity coverage. `build_prompt.mjs --still` fixed to cap at 1 Hazel canon anchor + 1 per NPC (was stacking full sets). | Clip 3 still: `e19eead1` (14 refs, FAILED) → `9f34b458` (4 refs, FAILED) → `9a0a6328` (2 refs, SUCCESS) | 2026-08-29 |
| N8 | **~50% of a multi-candidate `nano_banana_2`/`nano_banana_pro` batch can render the ENTIRE composition rotated ~90° (portrait canvas, landscape-oriented content) with no error and no distinguishing field in the result — only a visual check catches it.** Found on Clip 8 round 2: 2 of 4 candidates (A, D) came back fully rotated (horse/riders sideways, horizon vertical) despite an explicit `aspect_ratio: "9:16"` param and a portrait-only prompt; the other 2 candidates from the identical call rendered correctly oriented. Round 3 added an explicit `FRAMING/ORIENTATION HARD CONSTRAINT` naming the specific prior failure ("two of round 2's four candidates rendered rotated ~90 degrees... do not rotate for any reason") and 0 of 4 round-3 candidates rotated — consistent with this project's general pattern (S14/G1) that naming the SPECIFIC prior failure explicitly, not just asking for the correct default, is what actually suppresses it. **Compounding technique confirmed the same round: two hard constraints that each independently fixed a different candidate's single defect in round 2 (footwear correct in one, V-mode framing correct in the other) were combined as two separate, equally emphatic HARD CONSTRAINT blocks in one round-3 prompt, and a single candidate (D) satisfied both simultaneously plus nailed the eyeline** — confirms hard constraints stack additively in this pipeline rather than trading off against each other. | Clip 8 still round 2: `56be6030`(A, rotated)/`8a4cdb86`(B, correct, V-mode dropped)/`25424535`(C, nsfw-flagged)/`e67226f7`(D, rotated, but wardrobe/pose correct) → round 3 with combined+orientation constraints: `cde524d4`(A)/`a1c08663`(B)/`d5782e9b`(C, nsfw again)/`9f49fbf6`(D, ACCEPTED, owner-approved) — 0 of 3 rendered candidates rotated | 2026-08-31 |

### gemini-3-pro-image / gemini-3.1-flash-image (stills — mandatory per owner lock 2026-08-30, see CLAUDE.md)
| # | Finding | Evidence | Date |
|---|---|---|---|
| G1 | **A dense, mottled, full-frame-width texture band at the exact seam where a sharp foreground subject meets a hazy midground is a strong, repeatable Gemini rendering bias for this kind of composition — not a wording gap, and not fixable by asking Gemini to remove/avoid it.** Found on 6b's ground-level insert (horse legs over running soldiers' legs, dust haze between them): the band read as a solid "shelf," creating a "two floors" perception (owner-flagged twice) because it spanned the entire frame width uninterrupted and nothing ever visually crossed it. FOUR structurally different Gemini prompting strategies all reproduced the identical band: (1) direct edit ("PHOTO EDIT, keep everything identical except this defect"), (2) the same edit with the artifact explicitly named/described in detail, (3) a fresh non-edit generation told not to copy the reference's structure at all (this attempt also separately broke into a two-panel collage and reintroduced a visible rider — a different failure), (4) the same fresh-generation prompt on a DIFFERENT model (`gemini-3.1-flash-image` instead of `gemini-3-pro-image`) — nearly pixel-identical band, ruling out "which Gemini model" as the variable. **Root cause is structural, not textural: texture vs. blur treatments of the same full-width band shape both still read as a floor, because blur only disguises the surface — it doesn't break the shape.** **Working fix (deterministic compositing, not further AI generation): (a) vertically stretch the foreground subject's region (top-anchored resize, ~1.4x) so it genuinely extends into/overlaps the midground subject's zone, closing the gap that needed any "dust" treatment in the first place; (b) hand a SINGLE narrow, tightly-scoped Gemini edit ("this composition is correct and final, do not redesign anything, blend ONLY this one thin seam line") to clean up the resulting seam — this succeeds where full-artifact-removal asks failed because it's a genuinely small, well-defined retouch, not "invent a different rendering of this zone."** Manual PIL-only attempts at the same seam (Gaussian blur, organic noise-masked blur, brightness-matched blend, sharpness-matched blend) all left a visible discontinuity band no matter how the parameters were tuned — mismatched sharpness between a resized/upsampled region and untouched original content shows through a wide alpha blend regardless of feathering; only the final Gemini narrow-retouch pass produced a genuinely seamless result. | v5 (defect present, pre-existing before any of this session's fixes) → 4 direct-edit variants (`kueVtgTkS0GrgUakGSIe` etc., band unchanged) → 4 artifact-named variants (`7pArQO84MMsQvvIDMBlh` etc., band unchanged) → 4 fresh-generation variants (`X54JCC2eF5QNsDegzkNU` etc., band unchanged / one variant collage+rider) → 4 flash-model variants (`EvaLAhm861way1PU1IXZ` etc., band unchanged) → stretch+blend composite (PIL, seam visible) → Gemini narrow seam-blend (`H8DmQbhxXXWMGb20fWzY`, ACCEPTED, owner-approved) | 2026-08-30 |
| G2 | `gemini-3-pro-image` DOES support an explicit `aspect_ratio` model parameter (values incl. `9:16`, default `16:9`) — but it is not exposed on the simplified `creative_edit_image` tool call, only via `creative_add_flow_node`/`creative_update_node`'s `model_parameters`. Omitting it silently defaults to 16:9 landscape even when the prompt text explicitly requests "vertical 9:16" and the reference image is vertical. Once `model_parameters: {"aspect_ratio":"9:16"}` is set explicitly, results come back correctly sized. A first attempt to fix an already-16:9 result by asking Gemini to "extend/outpaint" it to 9:16 (rather than regenerating natively with the parameter set) produced a visible seam where the added top/bottom strips met the original crop — regenerating natively in the target aspect ratio from the start avoids this splice problem entirely. | 16:9 default: 4 variants at 1376×768 → outpaint-to-9:16 attempts (visible seam, rejected) → native 9:16 regeneration with `aspect_ratio` param set (clean, 1536×2752) | 2026-08-30 |

### Cross-model
| # | Finding | Evidence | Date |
|---|---|---|---|
| E1 | **Root cause of this project's persistent identity drift, found after owner-directed research 2026-08-30: production never used Higgsfield's purpose-built character-consistency feature.** The whole episode (and the v4/v5 Hazel canon build before it) relied on raw `image_references` job IDs + a long "STRICT IDENTITY RE-RENDER" prose block, re-matched fresh on every generation — the method CHARACTER_LOCK.md itself already documented as having "a proven ~90–95% identity ceiling," consistent with N5's "4 of 6 renders drifted" finding. Higgsfield's actual documented workflow is train Soul → save as Element → embed `<<<element_id>>>` directly in the prompt text (auto-injected, rewritten to `@element_name` by the backend) — this project had a Soul trained once (2026-08-20, on the now-archived v4 face) but never converted it to an Element or updated it for v5, and never used Elements at all. **Fix: created fresh Elements from the CURRENT v5/episode canon** — Hazel-v5 (`0dc0f31b-5fdc-4d85-bb2b-1a943eb3ca36`), Alexander-Gaugamela (`2742dd32-0b11-4a5c-9592-ad33228fc91b`), Parmenion-Gaugamela (`1b98b608-08d0-4a4e-81e2-aba119d984e7`), Bucephalus (`08dec96c-bf43-448d-a3f3-e17c812f172c`) — and made the placeholder mechanism PRIMARY in `build_prompt.mjs` (see `elementAnchorBlock`), with the old prose identity block kept only as belt-and-suspenders, secondary text. **Verified working on both models this episode uses:** nano_banana_pro still (job `cb4f75b5`) held identity with almost no prose at all; seedance_2_5 video (job `113edb7c`, 4s) accepted the placeholder cleanly and held identity with NO drift between first and last frame — notable since Higgsfield's own docs name "Seedance 2.0" for Element reuse, not 2.5, so this compatibility was not assumed, it was tested. **Still unverified:** multi-character combination (multiple `<<<id>>>` placeholders in one prompt, e.g. Hazel+Alexander together) — Higgsfield-documented as supported but not yet confirmed by an actual generation in this project; confirm on the next multi-character clip built this way before treating it as fact. | test still `cb4f75b5-6021-4625-8582-febe61077158`; test video `113edb7c-2ab3-48f3-bd2d-37a08390279a` | 2026-08-30 |
| E2 | **E1's plain v5-canon Element still isn't a perfect fix on its own — it drifted toward a slimmer/sculpted face and too-golden complexion versus the actual v5 lock spec, needing ~8 correction rounds before the owner accepted one.** Notable sub-findings from those rounds: (1) including a reference photo in a "photo edit, change only X" call carries real risk that the model substitutes the ENTIRE reference photo (pose/expression/lighting/background all copied) instead of surgically editing the base — happened once (job `751eeaee`/`a15591da`-adjacent round) when a full portrait reference was passed without an explicit "do not copy pose/lighting/background" guard; adding that guard explicitly fixed it on the next attempt. (2) Face-shape (roundness/cheek-fullness) specifically resisted correction across MULTIPLE different techniques — abstract instruction, concrete-magnitude instruction, reference-guided edit, and a fresh full-reference render all produced only subtle-to-moderate change, consistent with N6's "big anatomical asks render subtler than requested" finding, now confirmed to extend beyond eye size to overall face shape. (3) No dedicated face-warp/liquify/mesh-reshape tool exists in this project's available toolset (checked `4be4433d` and `a0a16113` MCP servers) — only different generation models to route the same prompt-based edit through; this was surfaced to the owner rather than silently assumed. **Fix accepted:** the corrected image was added as a 6th reference into a new "Hazel-v5.1" Element (`34bb316b-c5c4-4138-86ce-73bd96b80457`), now primary in `build_prompt.mjs`. | round: `e93d335a`(v2, complexion flagged)→`0a8ebebb`(v3, complexion fixed but flush too strong)→`8278349c`(v4, no visible face-shape change)→`a15591da`(v5, reference caused full-photo substitution, REJECTED)→`94acb1c0`(v6, text-only, real moderate change, owner-accepted as final)→`36cd0783`(v7)→`ed5c86dc`(v8)→`751eeaee`(v9, fresh full-reference render, still no change)→`53e8e49f`(v10, front-face-only reference) | 2026-08-30 |
| X1 | Raw historical numbers in prompts do nothing useful; translated density language + layering is what renders scale. | brief §7b.6; all battle jobs | 2026-08 |
| X2 | Every video clip gets §26 freezedetect + frame-extraction QC before delivery notes; motion defects between frames need eyes (owner watch, or Gemini eyes on TEST clips — owner-approved exception 2026-08-29). | standing | — |
| X5 | **Correct grade/lighting text in the prompt does not guarantee the actual generation matches it — the output must be visually checked against the episode's established look before delivery, not just the prompt text re-read.** 6b's start-frame still v1 was built with the same "harsh, flat, bright daylight" grade text used successfully on every other battle shot, but came back noticeably darker/dimmer/moodier — a real exposure drift the prompt text gave no warning of. Fixed via a targeted photo-edit (exposure/lighting only, composition/pose locked) rather than a full regeneration. Owner-directed standing rule from this finding: visual continuity (lighting, exposure, dust color, grade) is checked against the pixels of the actual result on every generation, not inferred from the prompt being correct — see CLAUDE.md MASTER RULE checklist. | v1 `96784d76` (off-grade) → v2 `35a00ea6` (exposure-fixed, owner-approved) | 2026-08-30 |
| X4 | **`seedance_2_5` real generation accepts only ONE `audio_references` entry — a second one fails the actual submission with a bare 422 (no detail in the error body), even though `get_cost:true` preflight validates fine with two.** This overturns the dual-reference method (voice-identity lock + tempo-pacing reference together) used earlier this episode for Alexander/Hazel clips — that combination silently worked before via the old `higgsfield` MCP tool surface, but failed here on the reconnected `4be4433d` server. Isolated by binary search: start_image + 2 audio_references → 422 on real submit; start_image + 1 audio_references → succeeds. Mitigation used on 5b: kept only the higher-priority VOICE IDENTITY/TIMBRE lock reference, dropped the tempo reference, relied on the DIALOGUE block's own text-based pacing instruction (already required as belt-and-suspenders per the ElevenLabs pacing rule) to carry tempo instead. Needs re-verification before assuming it's fixed — may be a transient server-side issue tied to which MCP connection is live, not a permanent model limitation. | 5b test: 1-audio-ref job `0c39f237` (succeeded) vs 2-audio-ref real submit (422, no job created) | 2026-08-30 |
| X3 | `nano_banana_pro` silently defaults `resolution` to `2k` if the param is omitted — no warning. The owner's permanent 4K lock (2026-08-29) was missed on Clip 2's v2 start-frame still because that still was hand-assembled outside `build_prompt.mjs` (which only ever built video payloads) and the param was never typed. Fix: `build_prompt.mjs --clip N --still` now exists specifically so no start-frame still is ever hand-assembled again — it hardcodes `resolution:"4k"` and auto-includes `HAZEL_CANON_4K` alongside the episode-costume refs. Rule: any still generation NOT run through this path is itself the defect, regardless of what resolution param gets typed by hand. | Clip 2 still v2, job `4b927706` (2K, caught by owner); fixed via `upscale_image` to 4K, job `6fa1e2f1` | 2026-08-29 |

---

## EXTERNAL GUIDANCE — Seedance 2.5 official/community prompting (researched 2026-08-29; UNVERIFIED in-project until a generation confirms — promote items up to the verified table as evidence lands)

Sources: fal.ai's Seedance 2.5 prompting guide (fetched in full), plus search-level content from
Kapwing/OpenArt/Dreamina guides and the official BytePlus ModelArk guide (docs.byteplus.com —
egress-blocked in this environment; formula corroborated across multiple secondary sources).

1. **@-tag reference syntax (the official dialect we have NOT been using):** references are
   addressed in the prompt as `@Image1`, `@Video1`, `@Audio1`, in media order. Assign each ONE
   narrow job, with an explicit do-not-copy clause:
   `"@Image1 controls only [identity/wardrobe/environment]. Do not copy [pose, background, lighting, camera angle] from @Image1."`
   Never give two references overlapping jobs — reference conflicts degrade output fast.
2. **The full modular template** (fill selectively — a 2s action doesn't need all blocks):
   `FORMAT` (duration, AR, single take, real-time) → `REFERENCE ROLES` → `STARTING STATE` →
   `TIMELINE` (time-coded beats) → `CAMERA` → `CONTINUITY` (invariants) → `AUDIO` → `ENDING STATE` →
   `CONSTRAINTS` (negation list).
3. **Static camera official phrasing:** "The camera never moves." / "The camera holds a [position]."
   One camera instruction only — stacked moves ("dolly in while panning left…") produce jitter.
4. **Cause before reaction, always:** write contact → resulting movement → sound → reaction, in
   that order; otherwise reactions can precede causes or fire simultaneously crowd-wide.
5. **ENDING STATE matters:** name the exact final positions — prevents drift/aimless motion in the
   clip's tail.
6. **Constraint-list style negatives DO work at the action/structure level** (distinct from S3's
   fine-costume-detail failure): "No cuts, no slow motion, no repeated action, no duplicated
   props, no object teleportation, no music." Use for structure; use positive description for
   costume/appearance details.
7. **Duration is time, not events:** long durations with one small action turn into waiting,
   repetition, or slow motion. Match event density to duration; don't overstuff 6s either.
8. **Occlusion = reset risk:** anything that disappears behind an obstacle needs its identity,
   clothing, direction, and speed RESTATED for when it re-emerges.
9. **Iteration discipline:** when a result is mostly right, rewrite only the physical state where
   the bad action begins and where it should end — one controlled change per retry, never a
   full-prompt rewrite (mirrors §19's one-attempt-one-report cadence).
10. **Continuity chaining:** extract the FINAL FRAME of an approved clip and pass it as the image
    reference for the next clip's generation — strongest cross-clip world-state carry.
11. **Vague performance adjectives ("dynamic", "epic") are close to useless** — corroborates brief
    §11.10's observable-behavior rule.
12. **Dialogue blocking:** spoken lines in straight quotes with start/end times; name who speaks
    AND who keeps their mouth closed; block silence explicitly ("3–6s: she stops speaking…").
    Audio palette as a flat list ending with "no music" when silent-scored.

**Adoption plan:** the episode's `build_prompt.mjs` (pai-pro-tooling/alexander/) implements the
template order, @-tag reference roles, static-camera phrasing, constraint lists, and
cause-before-reaction structure. First production clip validates items 1–6 → promote to verified.

## EXTERNAL TOOLS OUT OF REACH — Angles 2.0 / SHOTS (owner standing rule, see CLAUDE.md)

Higgsfield's **Angles 2.0** (one photo → a directed new camera angle) and **SHOTS** (one photo → 9
AI-picked angle options) would materially help exactly the kind of angle-finding problem S12
describes — but both are website-only, verified unreachable from this environment (not in the
MCP `apps_search` registry; a sandboxed/local Playwright browser is always an anonymous session
with no path to the owner's login, confirmed 2026-08-29, regardless of browser engine).
**Permanent rule (owner lock 2026-08-29): whenever a shot would benefit from either tool, tell
the owner at the point the need arises — he runs it himself and sends back the result.** Never
silently work around this, never skip it. Full rule: `CLAUDE.md`.
